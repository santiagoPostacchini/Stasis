
namespace Audio.Scripts {
    /// <summary>
    /// Marcador para scripts que exponen eventos (public event Action ...)
    /// que pueden triggerear audio.
    /// </summary>
    public interface ISoundPlayer { }
}